package MenuMain;

public class Menu {
    //ArrayList of Menu Items

    //Date last updated

    //Constructor that initializes the fields

    //addMenuItem() method which takes an instance of MenuItem and adds it to the menu AND updates lastUpdated

    //removeMenuItem() method which takes a menuId int and finds that MenuItem and removes it from the ArrayList AND updates lastUpdated

    //need getters for the two variables
}
